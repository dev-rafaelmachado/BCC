#include <stdio.h>

int main() {
  float x,y,a;

  
  printf("Digite o primero valor:");
  scanf("%f", &x);
  
  printf("Digite o primero valor:");
  scanf("%f", &y);

  
  printf("%2.2f / %2.2f = %3.2f ",x,y,x/y);
  
  return 0;
}